import './bootstrap';

/*
 | Alpine.js ships with — and is booted by — Livewire 3, so it must NOT be
 | imported/started here as well (doing so double-registers Alpine).
 |
 | Register project-wide Alpine plugins/components via the livewire:init event:
 |
 |   document.addEventListener('alpine:init', () => {
 |       Alpine.data('example', () => ({ ... }))
 |   })
 */
